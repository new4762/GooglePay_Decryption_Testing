# Google Pay environment mode: use "test" for sandbox or "prod" for production
GOOGLE_PAY_ENV=test

# Google Pay merchant identifier (e.g., gateway:your_gateway_name or merchant:your_merchant_id)
GOOGLE_PAY_MERCHANT_ID=gateway:your_gateway_name

# Google Pay private key (Base64-encoded PKCS#8 EC private key — no newlines, single-line only)
GOOGLE_PAY_PRIVATE_KEY=MIGHAgEAMBMGByqGSM49AgEG...your_base64_encoded_key...

# Google Pay payload (JSON string — must be a single line, with escaped quotes if needed)
GOOGLE_PAY_PAYLOAD={"signature":"...","protocolVersion":"ECv2","signedMessage":"..."}
